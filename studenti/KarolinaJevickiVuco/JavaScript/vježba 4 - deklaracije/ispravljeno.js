/**
// Inicijalizacija varijabli?
*/
var first_name = "Tony";

var first_name = "John",     //varijabla first name je redefinirana
lastName = Doe,
price = 0,
discount = 78,
fullPrice = 0,
break1 = [],    //break se ne može koristiti kao ime varijable jer je to jedna od ključnih riječi koje se upotrebljavaju u JS
$myObject = {};

console.log(price);  //pisano velikim slovima što nije isto što i malim slovima price
console.log(lastName); //name mora ići velikim slovima

// Kraj mog programa

// u vježbi na GIT hubu ono što je potcrtano crvenim slovima ne valja